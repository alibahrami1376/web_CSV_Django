# مستندات کامل: WEBSITE CONFIGURATION

## 📁 ساختار
```
website/
├── __init__.py
├── settings.py          # تنظیمات اصلی Django
├── urls.py              # URL routing اصلی
├── wsgi.py              # WSGI configuration
└── asgi.py              # ASGI configuration
```

---

## 📄 فایل: settings.py

### خط 1-14: Import ها و توضیحات
```python
"""
Django settings for website project.

Generated by 'django-admin startproject' using Django 5.2.8.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.2/ref/settings/
"""
import os
from pathlib import Path
from decouple import config, Csv
```

**توضیح:**
- `decouple`: کتابخانه برای مدیریت environment variables
- `config`: خواندن از فایل .env
- `Csv`: تبدیل رشته CSV به لیست

---

### خط 16-17: BASE_DIR
```python
# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent
```

**توضیح:**
- `Path(__file__)`: مسیر فایل settings.py
- `.resolve()`: تبدیل به مسیر مطلق
- `.parent.parent`: دو سطح بالا (root پروژه)
- استفاده: `BASE_DIR / 'templates'`

---

### خط 23-24: SECRET_KEY
```python
# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = config('SECRET_KEY', default='django-insecure-@i57f*3nduso2gldjq7-sm9tsdh0bqc5sa$^-!a7ngax(k8w2g')
```

**توضیح:**
- `SECRET_KEY`: کلید مخفی برای امنیت Django
- `config('SECRET_KEY', default=...)`: خواندن از .env یا استفاده از default
- **⚠️ مهم**: در production باید از .env استفاده شود

---

### خط 27-28: DEBUG
```python
# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = config('DEBUG', default=False, cast=bool)
```

**توضیح:**
- `DEBUG`: حالت debug
- `default=False`: پیش‌فرض False (production)
- `cast=bool`: تبدیل به boolean
- **⚠️ مهم**: در production باید False باشد

---

### خط 30: ALLOWED_HOSTS
```python
ALLOWED_HOSTS = config('ALLOWED_HOSTS', default='', cast=Csv())
```

**توضیح:**
- `ALLOWED_HOSTS`: لیست دامنه‌های مجاز
- `cast=Csv()`: تبدیل رشته CSV به لیست
- مثال در .env: `ALLOWED_HOSTS=example.com,www.example.com`
- **⚠️ مهم**: در production باید تنظیم شود

---

### خط 33-47: INSTALLED_APPS
```python
INSTALLED_APPS = [
    'multi_captcha_admin',
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'captcha', 
    'home',
    'blog',
    'projects'
]
```

**توضیح خط به خط:**

**خط 35:** `'multi_captcha_admin'`
- کپچا برای admin panel

**خط 36:** `'django.contrib.admin'`
- پنل مدیریت Django

**خط 37:** `'django.contrib.auth'`
- سیستم احراز هویت

**خط 38:** `'django.contrib.contenttypes'`
- سیستم content types

**خط 39:** `'django.contrib.sessions'`
- مدیریت session

**خط 40:** `'django.contrib.messages'`
- سیستم پیام‌ها

**خط 41:** `'django.contrib.staticfiles'`
- مدیریت فایل‌های static

**خط 42:** `'captcha'`
- کپچا برای فرم‌ها

**خط 43-45:** App های پروژه
- `'home'`, `'blog'`, `'projects'`

---

### خط 49-57: MIDDLEWARE
```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]
```

**توضیح خط به خط:**

**خط 50:** `SecurityMiddleware`
- تنظیمات امنیتی (HSTS, SSL redirect, etc.)

**خط 51:** `SessionMiddleware`
- مدیریت session

**خط 52:** `CommonMiddleware`
- عملکردهای مشترک (APPEND_SLASH, etc.)

**خط 53:** `CsrfViewMiddleware`
- محافظت CSRF

**خط 54:** `AuthenticationMiddleware`
- اضافه کردن `request.user`

**خط 55:** `MessageMiddleware`
- مدیریت پیام‌ها

**خط 56:** `XFrameOptionsMiddleware`
- محافظت clickjacking

**⚠️ ترتیب مهم است!**

---

### خط 59: ROOT_URLCONF
```python
ROOT_URLCONF = 'website.urls'
```

**توضیح:**
- فایل اصلی URL configuration
- Django از اینجا شروع می‌کند

---

### خط 61: LOGIN_URL
```python
LOGIN_URL = '/login/'
```

**توضیح:**
- URL برای redirect کاربران غیر لاگین
- وقتی `@login_required` استفاده می‌شود

---

### خط 63-65: MULTI_CAPTCHA_ADMIN
```python
MULTI_CAPTCHA_ADMIN = {
    'engine': 'simple-captcha',
}
```

**توضیح:**
- تنظیمات کپچا برای admin
- استفاده از simple-captcha

---

### خط 67-80: TEMPLATES
```python
TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'templates'],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]
```

**توضیح خط به خط:**

**خط 69:** `'BACKEND': 'django.template.backends.django.DjangoTemplates'`
- استفاده از Django template engine

**خط 70:** `'DIRS': [BASE_DIR / 'templates']`
- مسیر پوشه templates اصلی

**خط 71:** `'APP_DIRS': True`
- جستجو templates در پوشه templates هر app

**خط 73-77:** `'context_processors'`
- Processors که به همه templates دسترسی دارند:
  - `request`: object request
  - `auth`: اطلاعات کاربر (`user`, `perms`)
  - `messages`: پیام‌های Django

---

### خط 82: WSGI_APPLICATION
```python
WSGI_APPLICATION = 'website.wsgi.application'
```

**توضیح:**
- WSGI application برای deployment

---

### خط 85-93: DATABASES
```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}
```

**توضیح:**
- `ENGINE`: موتور دیتابیس (SQLite)
- `NAME`: مسیر فایل دیتابیس
- **⚠️ برای production**: باید PostgreSQL یا MySQL استفاده شود

---

### خط 96-112: AUTH_PASSWORD_VALIDATORS
```python
AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]
```

**توضیح:**
- Validator های رمز عبور:
  - `UserAttributeSimilarityValidator`: بررسی شباهت با username
  - `MinimumLengthValidator`: حداقل طول
  - `CommonPasswordValidator`: بررسی رمزهای رایج
  - `NumericPasswordValidator`: بررسی فقط عدد نبودن

---

### خط 115-124: Internationalization
```python
LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True
```

**توضیح:**
- `LANGUAGE_CODE`: زبان پیش‌فرض
- `TIME_ZONE`: منطقه زمانی
- `USE_I18N`: فعال‌سازی internationalization
- `USE_TZ`: استفاده از timezone-aware datetimes

---

### خط 127-137: Static & Media Files
```python
STATIC_URL = 'static/'
STATIC_ROOT = BASE_DIR / 'static'
STATICFILES_DIRS = [
    BASE_DIR / "statics",
]

MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```

**توضیح خط به خط:**

**خط 130:** `STATIC_URL = 'static/'`
- URL prefix برای static files
- مثال: `/static/css/style.css`

**خط 131:** `STATIC_ROOT = BASE_DIR / 'static'`
- مسیر جمع‌آوری static files
- بعد از `collectstatic` همه فایل‌ها اینجا هستند

**خط 132-134:** `STATICFILES_DIRS`
- پوشه‌های اضافی برای static files
- Django از اینجا هم فایل‌ها را می‌خواند

**خط 136:** `MEDIA_URL = '/media/'`
- URL prefix برای media files
- مثال: `/media/profiles/avatar.jpg`

**خط 137:** `MEDIA_ROOT = BASE_DIR / 'media'`
- مسیر ذخیره فایل‌های آپلود شده

---

### خط 139-142: DEFAULT_AUTO_FIELD
```python
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
```

**توضیح:**
- نوع فیلد primary key پیش‌فرض
- `BigAutoField`: عدد بزرگ (64-bit)

---

## 📄 فایل: urls.py

```python
"""
URL configuration for website project.

The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/5.2/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  path('', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  path('', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function:  from django.urls import include, path
    2. Add a URL to urlpatterns:  path('blog/', include('blog.urls'))
"""
from django.contrib import admin
from django.urls import path,include
from django.conf import settings
from django.conf.urls.static import static

urlpatterns = [
    path('admin/', admin.site.urls),
    path("", include("home.urls", namespace="home")),
    path('blog/',include('blog.urls', namespace='blog')),
    path('projects/',include('projects.urls', namespace='projects')),
    path('captcha/', include('captcha.urls')) 
]

# فقط در حالت development static files را serve کن
if settings.DEBUG:
    urlpatterns += static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)
```

**توضیح خط به خط:**

**خط 17-20:** Import ها
- `path`: برای تعریف URL pattern
- `include`: برای شامل کردن URL های app های دیگر
- `static`: برای serve کردن static/media در development

**خط 22-27:** `urlpatterns`
- لیست URL patterns اصلی

**خط 23:** `path('admin/', admin.site.urls)`
- URL: `/admin/`
- Django admin panel

**خط 24:** `path("", include("home.urls", namespace="home"))`
- URL: `/` (صفحه اصلی)
- شامل کردن URL های home app
- `namespace="home"`: برای استفاده در template (`{% url 'home:home' %}`)

**خط 25:** `path('blog/',include('blog.urls', namespace='blog'))`
- URL: `/blog/...`
- شامل کردن URL های blog app

**خط 26:** `path('projects/',include('projects.urls', namespace='projects'))`
- URL: `/projects/...`
- شامل کردن URL های projects app

**خط 27:** `path('captcha/', include('captcha.urls'))`
- URL: `/captcha/...`
- شامل کردن URL های captcha app

**خط 30-33:** Static/Media در Development
- `if settings.DEBUG:`: فقط در حالت debug
- `static(...)`: serve کردن static files
- `static(...)`: serve کردن media files
- **⚠️ در production**: باید توسط web server (Nginx/Apache) serve شود

---

## 📄 فایل: wsgi.py

```python
"""
WSGI config for website project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'website.settings')

application = get_wsgi_application()
```

**توضیح خط به خط:**

**خط 10:** `import os`
- برای دسترسی به environment variables

**خط 12:** `from django.core.wsgi import get_wsgi_application`
- تابع برای ایجاد WSGI application

**خط 14:** `os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'website.settings')`
- تنظیم module settings
- `setdefault`: فقط اگر وجود نداشت، تنظیم می‌کند

**خط 16:** `application = get_wsgi_application()`
- ایجاد WSGI application
- این متغیر توسط web server (Gunicorn, uWSGI) استفاده می‌شود

**استفاده:**
```bash
gunicorn website.wsgi:application
```

---

## 📄 فایل: asgi.py

```python
"""
ASGI config for website project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'website.settings')

application = get_asgi_application()
```

**توضیح:**
- مشابه wsgi.py
- برای WebSocket و async support
- استفاده با Daphne یا Uvicorn

---

## 🔄 جریان کار URL

```
User Request → website/urls.py → App urls.py → View → Template
```

**مثال:**
```
GET /blog/1/
  → website/urls.py (path('blog/', include(...)))
  → blog/urls.py (path('<int:post_id>/', blog_detail))
  → blog/views.py (blog_detail)
  → blog/templates/blog/blog_detail.html
```

---

## 📝 نکات مهم

1. **DEBUG**: همیشه در production False باشد
2. **ALLOWED_HOSTS**: باید تنظیم شود
3. **SECRET_KEY**: باید از .env خوانده شود
4. **Static/Media**: در production توسط web server serve شود
5. **Database**: برای production از PostgreSQL/MySQL استفاده شود
6. **Middleware Order**: ترتیب مهم است
7. **Namespace**: برای جلوگیری از تداخل نام URL ها

---

## 🎯 خلاصه

این فایل‌ها شامل:
- ✅ تنظیمات اصلی Django
- ✅ URL routing اصلی
- ✅ WSGI/ASGI configuration
- ✅ مدیریت Static/Media files
- ✅ تنظیمات امنیتی
- ✅ مدیریت Environment variables

